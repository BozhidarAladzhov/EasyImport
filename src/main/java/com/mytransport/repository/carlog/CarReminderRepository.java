package com.mytransport.repository.carlog;

import com.mytransport.models.carlog.CarReminder;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CarReminderRepository extends JpaRepository<CarReminder, Long> {
}
